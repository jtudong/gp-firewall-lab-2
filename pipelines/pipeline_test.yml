resources:
  pipelines:
  - pipeline: main
    source: pipeline_main
    trigger:
      branches:
        include:
        - master
        - dev

pool:
  vmImage: ubuntu-latest

steps:
- script: |
    echo $(resources.pipeline.pipeline_main.subnetPrefix)